{{define "review_card"}}
<article class="community__item card">
    <div class="community__stars" aria-label="Valoración {{.Stars}}">{{.Stars}}</div>
    <p>"{{.Text}}"</p>
    <span>- {{.Author}}</span>
</article>
{{end}}
